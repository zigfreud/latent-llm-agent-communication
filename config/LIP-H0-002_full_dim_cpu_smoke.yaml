experiment_id: "LIP-H0-002"
experiment_name: "LIP-H0-002 Remote CPU Full-Dim Smoke"
device: "cpu"
output_dir: "runs/LIP-H0-002"
seed: 42

data:
  dataset_path: "datasets/LIP-H0-002/full_dim_smoke_shards"
  batch_size: 2
  num_workers: 0
  drop_last: true

model:
  input_dim: 2048
  hidden_dim: 1024
  output_dim: 4096

training:
  epochs: 1
  max_steps: 2
  learning_rate: 0.001
  weight_decay: 0.0
  resume: false

loss:
  temperature: 0.1
  lambda_mse: 0.1
