name: LIP-H0-003 Remote CPU From Latent Bundle

on:
  pull_request:
    branches:
      - feature/lip-v2-contrastive
  workflow_dispatch:
    inputs:
      latent_bundle_url:
        description: Optional URL to a latent bundle zip. Empty uses a synthetic bundle.
        required: false
        default: ""
        type: string
      latent_bundle_sha256:
        description: Optional SHA256 digest for latent_bundle_url.
        required: false
        default: ""
        type: string

permissions:
  contents: read

env:
  EXPERIMENT_ID: LIP-H0-003
  CONFIG_PATH: config/LIP-H0-003_real_tiny_cpu.yaml
  BUNDLE_DIR: datasets/LIP-H0-003/latent_bundle
  RUN_OUTPUT_DIR: runs/LIP-H0-003
  NUM_SAMPLES: "8"
  INPUT_DIM: "2048"
  OUTPUT_DIM: "4096"
  MAX_STEPS: "2"
  LATENT_BUNDLE_URL: ${{ github.event.inputs.latent_bundle_url || '' }}
  LATENT_BUNDLE_SHA256: ${{ github.event.inputs.latent_bundle_sha256 || '' }}

jobs:
  cpu-from-latent-bundle:
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install CPU-compatible dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install PyYAML numpy

      - name: Prepare latent bundle
        run: |
          set -euo pipefail
          rm -rf "$BUNDLE_DIR"
          mkdir -p "$BUNDLE_DIR"

          if [ -z "$LATENT_BUNDLE_URL" ]; then
            synthetic_dir="$RUNNER_TEMP/LIP-H0-003-synthetic"
            rm -rf "$synthetic_dir"
            mkdir -p "$synthetic_dir"

            python -m src.scripts.make_toy_shards \
              --output-dir "$synthetic_dir/shards" \
              --num-samples "$NUM_SAMPLES" \
              --input-dim "$INPUT_DIM" \
              --output-dim "$OUTPUT_DIM"

            python - <<'PY'
          import json
          import os
          from pathlib import Path

          synthetic_dir = Path(os.environ["RUNNER_TEMP"]) / "LIP-H0-003-synthetic"
          manifest = {
              "bundle_format": "lip_latent_bundle",
              "schema_version": 1,
              "experiment_id": os.environ["EXPERIMENT_ID"],
              "input_dim": int(os.environ["INPUT_DIM"]),
              "output_dim": int(os.environ["OUTPUT_DIM"]),
              "shards": [
                  {
                      "path": "shards/shard_0.pt",
                      "records": int(os.environ["NUM_SAMPLES"]),
                  }
              ],
          }
          with open(synthetic_dir / "manifest.json", "w", encoding="utf-8") as handle:
              json.dump(manifest, handle, indent=2)
          PY

            python -m src.scripts.package_latent_bundle \
              --shards-dir "$synthetic_dir/shards" \
              --manifest "$synthetic_dir/manifest.json" \
              --output-zip "$synthetic_dir/latent_bundle.zip"

            unzip -q "$synthetic_dir/latent_bundle.zip" -d "$BUNDLE_DIR"
          else
            download_path="$RUNNER_TEMP/LIP-H0-003_latent_bundle.zip"
            curl --fail --location --show-error "$LATENT_BUNDLE_URL" --output "$download_path"
            if [ -n "$LATENT_BUNDLE_SHA256" ]; then
              echo "${LATENT_BUNDLE_SHA256}  ${download_path}" | sha256sum --check -
            fi
            unzip -q "$download_path" -d "$BUNDLE_DIR"
          fi

      - name: Validate latent bundle
        run: python -m src.scripts.validate_latent_bundle --bundle-dir "$BUNDLE_DIR"

      - name: Run CPU trainer from latent bundle
        run: |
          python -m src.pipelines.trainer \
            --config "$CONFIG_PATH" \
            --experiment-id "$EXPERIMENT_ID" \
            --output-dir "$RUN_OUTPUT_DIR" \
            --max-steps "$MAX_STEPS" \
            --device cpu

      - name: Validate smoke outputs
        run: |
          python - <<'PY'
          import csv
          import json
          import os
          from pathlib import Path

          output_dir = Path(os.environ["RUN_OUTPUT_DIR"])
          required_files = [
              "metrics.json",
              "train_log.csv",
              "run_summary.md",
              "last_checkpoint.pth",
              "best_model.pth",
          ]
          missing = [name for name in required_files if not (output_dir / name).is_file()]
          if missing:
              raise SystemExit(f"Missing expected output files: {', '.join(missing)}")

          with open(output_dir / "metrics.json", "r", encoding="utf-8") as handle:
              metrics = json.load(handle)

          expected_experiment = os.environ["EXPERIMENT_ID"]
          if metrics.get("experiment_id") != expected_experiment:
              raise SystemExit(
                  f"Expected experiment_id={expected_experiment}, got {metrics.get('experiment_id')}"
              )

          max_steps = int(os.environ["MAX_STEPS"])
          steps_completed = int(metrics.get("steps_completed", 0))
          if steps_completed <= 0:
              raise SystemExit("Trainer completed zero steps")
          if steps_completed > max_steps:
              raise SystemExit(
                  f"Trainer completed {steps_completed} steps, exceeding max_steps={max_steps}"
              )

          with open(output_dir / "train_log.csv", "r", encoding="utf-8") as handle:
              rows = list(csv.DictReader(handle))
          if len(rows) != steps_completed:
              raise SystemExit(
                  f"train_log.csv has {len(rows)} rows, expected {steps_completed}"
              )

          print(f"Validated {steps_completed} smoke training step(s) in {output_dir}")
          PY

      - name: Append run summary
        if: always()
        run: |
          if [ -f "$RUN_OUTPUT_DIR/run_summary.md" ]; then
            cat "$RUN_OUTPUT_DIR/run_summary.md" >> "$GITHUB_STEP_SUMMARY"
          else
            {
              echo "# ${EXPERIMENT_ID} Remote CPU From Latent Bundle"
              echo
              echo "run_summary.md was not produced."
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload smoke run artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: LIP-H0-003-${{ github.run_id }}-remote-cpu-from-latent-bundle
          path: runs/LIP-H0-003
          if-no-files-found: warn
          retention-days: 7
